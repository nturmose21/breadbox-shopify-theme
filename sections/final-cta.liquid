{% assign cta_product = section.settings.product | default: collections.all.products.first %}
<section class="final-cta full-width" data-motion-section>
  <div class="final-cta__grid" aria-hidden="true"></div>
  <img class="final-cta__part final-cta__part--left" src="{{ 'component-servo.png' | asset_url }}" alt="" width="600" height="600" loading="lazy" data-parallax="-5">
  <img class="final-cta__part final-cta__part--right" src="{{ 'component-oled.png' | asset_url }}" alt="" width="600" height="600" loading="lazy" data-parallax="6">
  <div class="final-cta__content" data-reveal="scale">
    <p class="eyebrow">{{ section.settings.eyebrow | escape }}</p><h2>{{ section.settings.heading | escape }}</h2><p>{{ section.settings.text | escape }}</p>
    <a class="button button--yellow" href="{% if cta_product != blank %}{{ cta_product.url }}{% else %}{{ routes.all_products_collection_url }}{% endif %}">{{ section.settings.button_label | escape }} →</a>
  </div>
</section>

{% stylesheet %}
  .final-cta { min-height: 280px; position: relative; display: grid; place-items: center; overflow: clip; border-block: 4px solid var(--ink); background: var(--teal); isolation: isolate; }
  .final-cta__grid { position: absolute; inset: 0; opacity: .22; background-image: linear-gradient(var(--ink) 1px, transparent 1px), linear-gradient(90deg, var(--ink) 1px, transparent 1px); background-size: 30px 30px; }
  .final-cta__content { position: relative; z-index: 3; width: min(640px, calc(100% - 2rem)); padding: 2.25rem 1rem; text-align: center; }
  .final-cta .eyebrow { color: var(--blue-dark); }
  .final-cta h2 { font-size: clamp(2.45rem, 5vw, 4rem); }
  .final-cta__content > p:not(.eyebrow) { margin-inline: auto; max-width: 38ch; font-weight: 800; line-height: 1.35; }
  .final-cta__part { position: absolute; z-index: 2; width: min(22vw, 240px); height: 72%; object-fit: contain; opacity: .72; filter: drop-shadow(5px 7px 0 rgba(7,10,13,.55)); transform: translate3d(0, var(--parallax-y, 0), 0); }
  .final-cta__part--left { left: -1%; bottom: -16%; rotate: 13deg; }
  .final-cta__part--right { right: 0; top: -10%; rotate: -11deg; }
  @media (max-width: 680px) {
    .final-cta { min-height: 290px; }
    .final-cta__content { padding-block: 2rem; }
    .final-cta__part { width: 34%; height: 34%; opacity: .34; filter: drop-shadow(4px 5px 0 rgba(7,10,13,.38)); }
    .final-cta__part--left { left: -5%; bottom: -5%; }
    .final-cta__part--right { right: -4%; top: -3%; }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Final call to action",
  "settings": [
    { "type": "product", "id": "product", "label": "Product" },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "Start small. Build real." },
    { "type": "text", "id": "heading", "label": "Heading", "default": "Get the kit." },
    { "type": "text", "id": "text", "label": "Text", "default": "One box. Real Arduino projects. Beginner friendly." },
    { "type": "text", "id": "button_label", "label": "Button label", "default": "Get BREADBOX" }
  ],
  "presets": [{ "name": "Final call to action" }]
}
{% endschema %}
